defaults:
  - scope:
      path: "docs"
      type: "pages" # previously `page` in Jekyll 2.2.
    values:
      layout: "docs" # overrides previous default layout
      footer: false
      author: "Dhruv Techapps"
      menu: docs
  - scope:
      path: "examples"
      type: "pages" # previously `page` in Jekyll 2.2.
    values:
      layout: "examples" # overrides previous default layout
      footer: true
      author: "Dhruv Techapps"
      menu: examples

title: Data Entry
email: dhruv.techapps@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  Automate data entry job, Fill input field or click button or link anything anywhere. easy configure in few steps and work like PRO
  data entry is chrome extension developed with HTML, CSS, Javascript. Quickly configure your job using XPath and work like PRO..
rating: 4.4 out of 5
users: 1
api_key: iapifmceeokikomajpccajhjpacjmibe
baseurl: # the subpath of your site, e.g. /blog
url: https://getdataentry.com # the base hostname & protocol for your site, e.g. http://example.com
author: Dhruv, Dharmesh
twitter_username: dhruv-techapps
github_username: dhruv-techapps
version: 1.0.0
exclude:
  [
    "cpu",
    "test",
    "Gemfile",
    "Gemfile.lock",
    "node_modules",
    "vendor/bundle/",
    "vendor/cache/",
    "vendor/gems/",
    "vendor/ruby/",
  ]
collections:
  docs:
    output: true
plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-redirect-from
  - jekyll-seo-tag
# Markdown rendering
markdown: kramdown